# DevOps Class – QA (Code Only)

WebdriverIO + Cucumber E2E tests (headless Chrome via DevTools) **without** CI artifacts.
You add the pipeline, reports, retries, schedules, etc.

## Run locally
- `npm install`
- `npm run test:e2e`# devops-class-qa
